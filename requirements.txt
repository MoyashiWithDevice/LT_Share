# Cloudflare Python Workers 用の依存定義。
# `npx wrangler deploy` (Cloudflare Builds の既定デプロイコマンド) は
# この requirements.txt を読んで Pyodide バンドルを作る。
# pyproject.toml の dependencies と一致させること。
# (ローカルの `npm run deploy` == `pywrangler deploy` は uv.lock を使うが、
#  リモートビルドは uv.sync の .venv を見ないため本ファイルが必須)
flask==3.1.3
workers-runtime-sdk==1.8.2
