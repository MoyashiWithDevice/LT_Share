{
  "name": "lt-share",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:schema": "wrangler d1 execute lt-db --remote --file=./schema.sql",
    "secret:admin": "wrangler secret put ADMIN_PASSWORD"
  }
}
